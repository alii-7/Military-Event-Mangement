@model Tarbya.Models.EducationalQualification

@{
    ViewBag.Title = "Edit";
}

<div class="row">
    <div class="col-3 offset-9">
        <div class="box-title">
            <h3>تعديل مؤهل دراسى</h3>
        </div>
    </div>
</div>
<br>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="row">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)

        <div class="form-group col-4">
            <label>
                المؤهل الدراسى
            </label>
            <div>
                @Html.EditorFor(model => model.educationalQualificationName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.educationalQualificationName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group col-12">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="حفظ" class="btn btn-red col-4" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("العودة", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
